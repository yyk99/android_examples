package com.yyktools.thrustcalc2;

import org.junit.Test;
import static org.junit.Assert.*;

/**
 * Created by y.kuznetsov on 12/21/2017.
 */

public class CVSWriterUnitTest {
    @Test
    public void calculate_isCorrect() throws Exception {
        assertEquals(4, 2 + 2);

        //CVSWriter wrt = new CVSWriter("test.csv");

        //assertEquals("", wrt.getLastError());
        //assertTrue(wrt.isExternalStorageReadable());
        //assertTrue(wrt.isExternalStorageWritable());
    }
}
